using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class esfera : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hola, soy una esfera!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
